import { PasswordRegexDirective } from './password-regex.directive';

describe('PasswordRegexDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordRegexDirective();
    expect(directive).toBeTruthy();
  });
});
